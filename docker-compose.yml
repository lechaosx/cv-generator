services:
  backend:
    build:
      context: .
      target: dev
    environment:
      DOMAINS_CONFIG: ${DOMAINS_CONFIG:-/config/domains.json}
      DEFAULT_CV: /data/default.yaml
    volumes:
      - ./src:/app/src
      - ./config/domains.json:/config/domains.json:ro
      - ${DEFAULT_CV:-./data/cv.yaml}:/data/default.yaml:ro
      - diskcache:/data

  caddy:
    image: caddy:alpine
    ports:
      - 80:80
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - backend

volumes:
  diskcache:
