[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "diskcache>=5.6.0",
    "flask>=3.1.3",
    "gunicorn>=25.3.0",
    "requests>=2.33.1",
    "pyyaml>=6.0",
]
